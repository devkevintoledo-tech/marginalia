[pytest]
asyncio_mode = auto
testpaths = tests
# app/ is a namespace package (no __init__.py); put the backend root (rootdir)
# on sys.path so a bare `pytest` invocation can import it (without this, only
# `python -m pytest` works, since `-m` prepends the CWD).
pythonpath = .
